import React from 'react';
import { Percent } from './Percent';
export declare type LineProgressBarProps<T extends number> = {
    percent: Percent<T>;
    rounded?: number;
    stripe?: boolean;
    height?: number;
    width?: number;
    direction?: 'left' | 'right';
    className?: string;
    progressColor?: string;
    containerColor?: string;
    progressStyle?: React.CSSProperties;
    containerStyle?: React.CSSProperties;
    label?: (value: Percent<T>) => React.ReactNode;
};
export declare const LineProgressBar: <T extends number>({ percent, rounded, stripe, height, width, direction, className, progressColor, containerColor, progressStyle, containerStyle, label, }: LineProgressBarProps<T>) => JSX.Element;
